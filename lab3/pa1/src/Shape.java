/**
 *
 * 定义了一个名为 Shape 的抽象类，其中包含两个抽象方法
 * getPerim 和 getArea。这两个方法分别用于计算形状的周长和面积，
 * 但具体的实现需要在子类中提供。
 *
 *
 */
public abstract class  Shape {
    abstract double getPerim();
    abstract double getArea();
}
